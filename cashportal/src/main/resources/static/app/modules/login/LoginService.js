(function() {
	'use strict';
    angular
        .module('app.login')
        .factory('LoginService', LoginService);

    /* @ngInject */
    function LoginService($http) {
     

        var service = {
            
        };

        return service;

        

    }
})();